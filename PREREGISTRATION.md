# Preregistration

Primary endpoint: patient-held-out five-phase boundary timing error, `E_PH <= 0.90 E_base`. This code implements synthetic identifiability validation only. Phase labels must never be primary-model features. Thirty seeds, grouped patient splits, bootstrap CIs, F1–F7 tests, and matched-capacity SLDS are required before clinical claims.
